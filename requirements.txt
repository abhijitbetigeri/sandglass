wasmer-sdk      # Boundary 1: the capability sandbox around every agent
tenki           # the edge fleet: disposable microVMs we can partition and kill
agentmail       # audit + command transport across intermittent links
pyyaml
pynacl          # Ed25519 for the committee quorum signatures
